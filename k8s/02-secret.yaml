# Template only — fill in real values and apply with:
#   kubectl create secret generic kps-secrets --from-env-file=.env -n knowledge-product-studio
# or seal/encrypt this file with your cluster's secret manager (Sealed Secrets,
# External Secrets Operator, SOPS, etc) before committing a real version.
# Never commit actual credentials.
apiVersion: v1
kind: Secret
metadata:
  name: kps-secrets
  namespace: knowledge-product-studio
type: Opaque
stringData:
  POSTGRES_USER: "kps"
  POSTGRES_PASSWORD: "change-me"
  ANTHROPIC_API_KEY: ""
  OPENAI_API_KEY: ""
  CONFLUENCE_BASE_URL: ""
  CONFLUENCE_API_TOKEN: ""
  CONFLUENCE_USER_EMAIL: ""
  OIDC_ISSUER_URL: ""
  OIDC_CLIENT_ID: ""
  OIDC_CLIENT_SECRET: ""
  JWT_SECRET: "change-me"
